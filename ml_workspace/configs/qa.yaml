host: ${CLICKHOUSE_HOST}
user: ${CLICKHOUSE_USERNAME}
password: ${CLICKHOUSE_PASSWORD}
database: ${CLICKHOUSE_DATABASE}

symbol: WLFIUSDT
start: 2025-10-06T06:00:00Z  # Actual data start time
end: 2025-10-08T13:15:00Z    # Extended to cover all available data (max: 13:14:37)

tables:
  - bitget_l1           # BBO (Best Bid/Offer) data
  - bitget_ticker       # Ticker data

list_tables: true
overlap: true
top_gaps: 5

compat_profile: spot
schema_dump_tables: [bitget_l1, bitget_ticker, bitget_orderbook]
search_symbol: WLFI
search_tables: [bitget_l1, bitget_ticker]
# load_tests_hours: 72  # Disabled: requires spot_* tables that don't exist

# Disabled: feature table not yet created
# range_compare:
#   raw: hft_db.bitget_l1
#   feature: hft_db.simple_mid_term_features
#   symbol: WLFIUSDT
