# Rust 本地加速配置

[build]
# 開發時並行度 (根據 CPU 核數調整)
jobs = 8
# CI lanes that install sccache opt in through RUSTC_WRAPPER. Keeping the
# wrapper out of project config lets metadata and security tools run anywhere.
