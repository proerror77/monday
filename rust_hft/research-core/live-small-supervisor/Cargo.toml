[package]
name = "hft-live-small-supervisor"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
hft-promotion-gate = { path = "../promotion-gate" }
hft-research-manifest = { path = "../manifest" }
serde = { workspace = true, features = ["derive"] }
thiserror = { workspace = true }

[dev-dependencies]
chrono = { workspace = true, features = ["serde"] }
