[package]
name = "hft-infra-clickhouse"
version = "0.1.0"
edition = "2021"
description = "ClickHouse 整合：RowBinary 批量插入（旁路）"

[dependencies]
clickhouse = { workspace = true, optional = true }
serde = { workspace = true }
tracing = { workspace = true }

[features]
default = []
infra-clickhouse = ["dep:clickhouse"]

[lib]
name = "infra_clickhouse"

